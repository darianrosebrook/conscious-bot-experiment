{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    // Ensure build uses compiled workspace packages, not src path aliases
    "paths": {}
  },
  "exclude": [
    "src/skill-integration/**/*",
    "src/**/*.test.ts",
    "src/**/*.test.tsx",
    "src/__tests__/**/*"
  ]
}

