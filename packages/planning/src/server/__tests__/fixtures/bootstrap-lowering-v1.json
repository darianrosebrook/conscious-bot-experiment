{
  "_comment": "Canonical bootstrap lowering contract fixture — v1. Both Sterling (Python) and Planning (TypeScript) test suites assert against this shape. Changes require updating both sides. See docs/closeouts/autonomy_pipeline_hardening_closeout_2026-02-10.md §C for bump procedure.",
  "schema_version": "bootstrap_lowering_v1",
  "required_step_keys": ["leaf", "args"],
  "required_args_keys": ["lowered_from", "theme"],
  "cases": {
    "explore_surroundings": {
      "lemma": "explore",
      "theme": "surroundings",
      "steps": [
        {
          "leaf": "move_to",
          "args": {
            "target": "exploration_target",
            "distance": 10,
            "lowered_from": "explore",
            "theme": "surroundings"
          }
        }
      ]
    },
    "navigate_safety": {
      "lemma": "navigate",
      "theme": "safety",
      "steps": [
        {
          "leaf": "step_forward_safely",
          "args": {
            "distance": 8,
            "lowered_from": "navigate",
            "theme": "safety"
          }
        }
      ]
    },
    "gather_food": {
      "lemma": "gather",
      "theme": "food",
      "steps": [
        {
          "leaf": "acquire_material",
          "args": {
            "item": "sweet_berry_bush",
            "count": 1,
            "lowered_from": "gather",
            "theme": "food"
          }
        },
        {
          "leaf": "consume_food",
          "args": {
            "lowered_from": "gather",
            "theme": "food"
          }
        }
      ]
    }
  }
}
